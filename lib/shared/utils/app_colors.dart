import 'package:flutter/material.dart';

const blackColor = Colors.black;
const whiteColor = Colors.white;

const scaffoldBackGroundColor = Color.fromRGBO(255, 250, 244, 1);

const primaryColor = Color.fromRGBO(252, 243, 203, 1.0);

const darkGreyColor = Color.fromRGBO(51, 51, 51, 1.0); // RGB: (51, 51, 51)
const mediumGreyColor =
    Color.fromRGBO(102, 102, 102, 1.0); // RGB: (102, 102, 102)
const lightGreyColor =
    Color.fromRGBO(153, 153, 153, 1.0); // RGB: (153, 153, 153)
const extraLightGreyColor =
    Color.fromRGBO(204, 204, 204, 1.0); // RGB: (204, 204, 204)

const lightGrey = Color.fromRGBO(128, 125, 119, 1);

const orangeColor = Color.fromRGBO(237, 141, 54, 1);
